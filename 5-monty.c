#include "monty.h"
/**
 * nop_function - tis function doesn't do anything.
 * @stack: double pointer to the top of the stack.
 * @line_num: number.
 */
void nop_function(stack_t **stack, unsigned int line_num)
{
(void) line_num;
(void) stack;
}
